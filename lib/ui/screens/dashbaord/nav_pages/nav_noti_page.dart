import 'package:flutter/material.dart';

class NavNotificationPage extends StatelessWidget{
  const NavNotificationPage({super.key});


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Notification"),),
    );
  }

}